I Assistant: Kiro Aیسنده 1.0.0
نو01-21
نسخه:اریخ: 2025-

تمی‌کنند؟ار I ها ک AP  - آیاشوند؟
 S لود می‌ی CSS و Jل‌هااییا ف
   - آtwork:**سی ne **برر

4.OSFirefox i   - Chrome iOS
- i iOS
   ar- Saf**
   ای مختلف:مرورگرهت در 
3. **تسScript
 خطاهای Javaرسی - برConsole
  > 
   - F12 مرورگر:**ole onsرسی c

2. **بر``tend
   `ron fpose logs-comdocker  h
 `bas   ``ها:**
 لاگ‌ **بررسی

1.دارد:ی وجود 

اگر مشکل# عیب‌یابی
#ه‌اند
شد اعمال hancementsive en progresات با
- تغییررفته استنگتاثیر قرار رورگرها تحت  مد در سایرد
- عملکرible هستنrd compat backwaات- تمام تغییر نیست
ییر backend نیازی به تغ
-d هستند frontenرات فقط برایتغییین 

- ا## نکات مهمد)

d` (جدی_FIX.mDATE_PICKEROS_SAFARI_ `I6.h` (جدید)
ri_fix.sios_safaeploy_.ts`
5. `dignfeafletCoutils/ld/lib/ennts`
4. `froglobals.csd/app/nten`
3. `froction.tsximeSeleonents/DateTbooking/compnsfers/raocale]/tp/[lontend/ap. `frtsx`
2cker.LocationPirs/Mapransfets/td/componenen
1. `front یافته
یل‌های تغییر`

## فاe up -d
``poscker-com
doontendfrhe -no-cacse build -compodocker-wn
 doposecomr-ockebul
dplusistancd h


```bas دستی:
یا به صورت`
i_fix.sh
``arafos_sy_i./deplobul
plusistan
cd `bash
`` تغییرات:

برای اعمال Deploy
dscape

##t و lanحالت portrai تست در 
   -خ‌های مختلف تاری  - انتخابte picker
 ن da   - باز کردزوم
ز کنترل‌های تفاده ا - اس
  ب مکانتخانقشه برای انروی لیک  ک
   -خاص:**تست‌های ت

3. ** تسر سرویس‌هایack یا سایSt  - Browserدر Xcode
 lator mu - iOS Si  
per Toolsfari Develo   - Sa**
‌ساز iOS:یه
2. **شبنید
 کتستpicker را قشه و date نید
   - نng را باز کnsfers/bookitraصفحه  - بروید
  ه آدرس سایت 
   - با Safari iPad ب iPhone یاعی:**
   -iOS واق**دستگاه . 

1تغییرات:ای تست این ست

برک

## تفی هنگام کلیom تصاد
- ✅ عدم zo استiOS حفظ شدهive اهر nat ظ
- ✅نندیخ انتخاب کنند تارن می‌توابراد
- ✅ کارi باز می‌شوafarS Sدر iOکر تاریخ ل پی ✅ جدوcker:
-Date Pi# صفحه

##لکردهای ا سایر عمخل بدم تدا عت
- ✅یافته اسبهبود به تاچ جر- ✅ تد
ار می‌کنن کتیه درس‌های زوم بنترل✅ ک- ند
 کلیک کن نقشه‌توانند رویران iOS میه:
- ✅ کاربنقش### 
یرات:
 این تغیجه

با# نتی

#` }
}
``style);
 endChild(pp.aad document.he   `;
   
      }
       }portant;
  lateZ(0) !imsform: transran     tt;
     !importannslateZ(0)  transform:rawebkit-t        -ated {
  nimet-zoom-aleafl      .iOS */
  n on celeratio acrerce hardwa     /* Fo       
    }
   nt;
     on !importamanipulatih-action:    touc     tive {
  interact-leleaf    .   
    
             }ortant;
touch !impcrolling: overflow-s    -webkit-     ntainer {
  .leaflet-co{
       ne) : noouch-callout(-webkit-tupports     @s/
  es *eraction fix map intficri speciOS Safa     /* i     
   }
   t;
  ortanne !impcallout: noh-it-touc    -webk
    nt;taimporansparent ! trt-color:ghtap-highliit-     -webk{
   ainer * eaflet-cont
      .lling */i touch handfar iOS Sa/* Enhanced         
   }
 ;
     portantzoom !impan-y pinch-ch-action: tou
        tant;one !impor nect:  user-sel    rtant;
  !imponone t: selecwebkit-user-    -  t;
  e !importannonallout: ch-cit-tou      -webk
  rtant;poarent !imnsptracolor: highlight-t-tap-webki      -
  ntainer {coflet- .lea     HTML = `
e.innertyl
    s;le')ement('styeateEl.crent = docum style    const) {
efined't !== 'undcumenpeof do(ty{
  if xes() lyIOSFition apprt func

expo;
}   })
  }ibility
 compatr iOS tteberer for SVG rendefault  Use dedefined, //erer: unrend
      alse, fferCanvas:      pre && {
sIOSSafari..(i    . iOS
ng fordlied touch han// Enhanc  
   ? 20 : 15,afarice: isIOSSeran
    tapTolfari, isIOSSans
    tap:ecific optiospi ar  // iOS Saf
  n: true,iomAnimat  markerZoo  ,
: truedeAnimation
    faue,mation: trni   zoomAtrue,
 ontrol: ttributionC  a   false,
keyboard:    ,
 falsem:   boxZoo,
  truedragging:
    !isMobile,oom: scrollWheelZ    alse,
ckZoom: fdoubleClirue,
    oom: t  touchZ
   false,omControl:  zon {
   retur;

 .userAgent)st(navigator.tera Mini/iMobile|Opeerry|IEiPod|BlackB|iPad|bOS|iPhoned|wele = /Androionst isMobigent);
  cerAavigator.usst(n/Safari/.teS && ari = isIO isIOSSafnstcont);
  geserAavigator.u.test(nne|iPod/|iPhosIOS = /iPad const i) {
 tions(apOpileMction getMobt funxporypescript
e

```ttConfig.tseafleد l. بهبو

### 4
}
```ortant;
  }280 !impor: #6b7  colnt;
  %) !importateY(-50ransla tnsform: traant;
   !importop: 50%     tortant;
12px !imp:   rightnt;
  ta!impor absolute position:    portant;
inter !imsor: po;
    curmportantght: 20px !int;
    heiortah: 20px !imp
    widtt;importan16px 16px !e: und-siz   backgrot;
 portanenter !imrepeat c...") no-svg+xml,data:image/l("ound: ur  backgrportant;
  acity: 1 !imop    {
 ndicatorr-picker-it-calendawebki"date"]::-input[type=
  
   }n: ltr;
  directioeft;
   xt-align: l    te
e {ime-valu-tandbkit-date--weate"]::pe="dinput[ty
  }
  
  nt;portaone !imct: nleser-se  utant;
   !impor: noneselectit-user-  -webk
  important;lout: none !uch-cal -webkit-toortant;
   imprent !transpacolor: ght-hlit-tap-higbkint;
    -we!importaulation n: manipctio    touch-ant;
mportaht: 48px !i-heig
    min iOS */ zoom on; /* Preventtantmporpx !i-size: 16    fontmportant;
px 12px !ipx 40px 12ng: 12di padant;
   rt!imporem : 0.5order-radius   bant;
 mport !iolid #d1d5db s1pxder: 
    bor!important;d: white   backgroun
  important;ton !nulist-butearance: meppant;
    aton !importutlist-be: menuappearancit-
    -webkdate"] {" input[type=
 es */tyli specific s Safar* iOS
  /t: none) {touch-callouts (-webkit-
@supporFixes */icker ate PS Safari D
/* iO`css
``
)ls.cssCSS (globaرات ## 3. تغیی
#
```
 }}
/>
    }
 }     , error);
 fallback:'k icker cliclog('Date pnsole.        co (error) {
ch } cat
     
        }End), 50);ouchvent(thEput.dispatcut(() => insetTimeo    ;
      art)ouchStchEvent(tput.dispatin     
          );
             } 
   truelable:  cance   
       s: true,      bubble       
 end', {('touch TouchEventhEnd = newonst touc      c
              );
  }   
     nyta */] as auch daches: [/* to       toutrue,
     ncelable:       ca
      ue, es: tr     bubbl    rt', { 
   hsta'touchEvent(ew Touc nrt =st touchSta    con
      ch eventsic toupecif // iOS s      
            ckEvent);
 clispatchEvent( input.di  
             });
    w: window          vie true,
   ble:cancela           e, 
 les: trubb      bu
      k', { Event('clic new Mouset = clickEven    constnts
      atch eve dispeate and// Cr       
            ocus();
 .f    inputi
      ar iOS Safs forhodative met  // Altern   se {
             } elicker();
wPy).shoput as an  (in {
        ')nctionfu '===ker owPic any).shinput aspeof ( (ty    ifer
    e pick datpento ople methods y multi    // Tr  
          lement;
nputELIarget as HTM= e.tt input   consry {
          t      
  ion();
opPropagat     e.st
 Default();   e.prevent {
   ile)   if (isMob=> {
 ={(e) 
  onClickS Safarir for iOclick handle/ Enhanced }
  /    }
  }  }
);
    :', errorallback focus fker('Date pice.logonsol      c{
   (error)      } catch    }
     hEnd);
(toucpatchEventput.dis       in  );
 chStartouvent(t.dispatchE       inputrue });
   able: tancelles: true, c bubb {hend',vent('toucTouchEew  n =hEndtouc      const );
    le: true }abrue, cancel bubbles: t, {ouchstart''tvent( new TouchEuchStart =st to        confor iOS
  ch events tch tou// Dispa
          
          ut.click();inp  );
        .focus(nput        ievents
  and trigger s ocu/ Force f /         
se {} el);
        showPicker(s any).(input a       ion') {
   'functPicker === showas any).f (input  (typeo      ifElement;
  puts HTMLIn e.target aonst input = cy {
        tre) {
     (isMobil> {
    if us={(e) =
  onFocrtuppoi sed iOS Safar// Enhanc
  }}
      })'16px',
dSize: grounck      baenter',
 cpx12ht 'rigsition: ckgroundPo',
      bao-repeatdRepeat: 'nbackgroun
      ','noneage: ckgroundIm     bapx 12px',
  12px12px 40adding: '    p
  : '0.5rem',rderRadiusbo
      b',5dx solid #d1d'1pr: 
      borde,te': 'whibackground   {
   Mobile && 
    ...(islestyific si speciOS Safar// s
    venesesponsi r touchoveImpr/ lation', / 'manipuchAction:tou  ugh
  e enoget is largtar touch urens/ E48px', /minHeight: '
     on iOSrevent zoom // P4px',' : '1 '16pxsMobile ?: iizes
    fontSific styleile-specional mob   // Addit
 dex: 1,,
    zIn'relative'sition: 
    poileshow on mobcker to orce date pi',
    // Fn' : 'none-buttomenulistle ? 'sMobie: iranc,
    appeatfield': 'texrance   MozAppea 'none',
 t-button' :e ? 'menulisobilisMe: anckitAppear Webcker
   date pior es fcific fixari speS Saf // iO{
   style={"
  xt-gray-100te700 dark:k:bg-gray-parent darnstrar-rdeocus:bo00 fg-blue-5:rinusg-2 foclg focus:rind-dey-600 rounr-graorde300 dark:bray-r-grder borde-3 pl-10 bo"w-full pclassName=axDate}
    max={mnDate}
{mi
  min=lue)}arget.vage(e.t> onChan(e) =nge={
  onCha={value}ue"
  val"datetype=ut
  inpsx
<`tsx:

``Selection.t در DateTimeات
#### تغییرcker
 Date Piرفع مشکل2. 
```

### ns())}
>OptioMobileMap
  {...(getic optionsri specifSafa // iOS {20}
 ce=pToleran
  tap={true}
  taue}ng={tr
  draggirue}Zoom={teelscrollWh{false}
  eClickZoom= doublrue}
 m={t
  touchZoo={false}ontroloomC)}
  zing(falsepLoad() => setMay={adnRe }}
  whe'
 'noneserSelect: 
    ut: 'none',serSelec WebkitU,
   one'llout: 'nebkitTouchCa',
    Wpinch-zooman-y on: 'ptiAc
    touch'100%', th: id
    w%', : '100
    height style={{ oom}
 zoom={zter}
  {mapCencenter=
  ner
<MapContai`tsxer:

``ontain در MapCیرات
#### تغی
[map]);
```}
  }
},     }
    se });
  ive: falpass       }, { ();
 entDefaulteve.pr        e) => {
  hmove', (r('toucentListeneaddEvcontainer.  
      ;
        e })assive: fals    }, { plt();
    preventDefau         e.) => {
 (ehstart', ner('toucntListener.addEve      contai
  anceormperfr iOS r bettesteners foent lisive ev pasAdd/   /             
 e';
'nont = tUserSelecwebkityle.ontainer.s;
        cut = 'none'hCallooucyle.webkitTner.sttaion c       -zoom';
'pan-y pinchhAction = style.toucner.ntai
        co) {ontainer     if (ctainer();
 onmap.getCr = netaiconst con
       workh events toorce touc  // F    Safari) {
if (isIOSent);
    rAgusenavigator.est(/.tSafari) && /Agenttor.uservigat(natesd/.Phone|iPo/iPad|ifari = sIOSSa    const is
Safari fixe iOS d specific// Ad
    
    ();
    }.enabledragging
      map.{ging) ragif (map.d}
    e();
    oom.enabl.touchZ
      mapm) {p.touchZoo
    if (maiOS Safarievents for h ble toucorce ena  
    // F }
       }
   0;
 = 2e .tapTolerancns).tap.optio(map as any       ap) {
 .ts any)  if ((map aS
    r iOerance fotap tolase / Incre   /able();
   y).tap.en anmap as   () {
   tion'=== 'funcle tap?.enabs any). (map ap && typeof('tap' in man
    if onfiguratioith better cr iOS wap foe tEnabl   
    // e();
 disablZoom.ubleClickap.do
    m{  if (map) (() => {
ffectuseEent
React.ntsComponبهبود MapEve/ 
/sx``ttsx:

`ationPicker.MapLocتغییرات در 
#### شکل نقشه
### 1. رفع م

شدهعمال ای احل‌ه
## راه‌r
te pickeve daن natirigger کرد مشکل در t
-API)` wPicker(ز `shoپشتیبانی ادارد
- عدم ton` utst-blirance: menuebkit-appea`-wبه نیاز iOS Safari 
- :erPickte ts

### Datouch eveng برای handlinevent شکل در let
- مtap` در Leafای `دهداوینی از رم پشتیبا
- عدiiOS Safarی ion` برا`touch-actنامناسب یمات قشه:
- تنظ

### نمشکلات

## علت  کنندیخ انتخابانند تارنمی‌تو- کاربران ارد
fari مشکل د در iOS Sa="date"]``input[typeز نمیشود
- بایک  تاریخ با کلپیکر- جدول ection)
TimeSeler (Dateل Date Pick
### 2. مشک نمی‌کنند
‌های زوم کار- کنترلد
ک کننقشه کلیوانند روی نان نمی‌ت- کاربرراکت ندارد
 ولی اینتباز می‌شود
- نقشه icker)MapLocationPمشکل نقشه ( 1. اشت:

###اصلی وجود دمشکل ri iOS، دو ورگر Safaون و مرشی‌های آیف
در گو

## مشکل Safariشه در iOS و نقkerte Picکل Daمش# رفع 